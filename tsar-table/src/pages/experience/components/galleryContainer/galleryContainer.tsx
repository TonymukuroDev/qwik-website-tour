import { component$ } from "@builder.io/qwik";


const GalleryContainer = component$(() => {
    return (
        <section class="gallery-container">Gallery Container</section>
    )
});

export default GalleryContainer;