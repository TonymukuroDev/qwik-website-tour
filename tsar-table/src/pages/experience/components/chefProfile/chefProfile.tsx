import { component$ } from "@builder.io/qwik";


const ChefProfile = component$(() => {
    return (
        <section class="chef-profile">Chef Profile</section>
    )
});

export default ChefProfile;