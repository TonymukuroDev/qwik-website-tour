import { component$ } from "@builder.io/qwik";


const StorySection = component$(() => {
    return (
        <section class="story-section">Story Section</section>
    )
});

export default StorySection;