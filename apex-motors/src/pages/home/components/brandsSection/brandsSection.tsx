import { component$ } from "@builder.io/qwik";
import './brandsSection.scss';

const BrandsSection = component$(() => {
    return (
        <section class="brands">Brands Section</section>
    )
});

export default BrandsSection;