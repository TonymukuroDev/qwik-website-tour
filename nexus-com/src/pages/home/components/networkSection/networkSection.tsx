import { component$ } from "@builder.io/qwik";


const NetworkSection = component$(() => {
    return (
        <section class="network">Network Section</section>
    )
});

export default NetworkSection;