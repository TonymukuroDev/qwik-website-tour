import { component$ } from "@builder.io/qwik";


const StatsBarSection = component$(() => {
    return (
        <section class="stats-bar">Stats Bar Section</section>
    )
});

export default StatsBarSection;